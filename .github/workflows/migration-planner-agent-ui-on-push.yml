name: migration-planner-agent-ui-on-push

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  wait:
    runs-on: ubuntu-latest
    steps:
      - name: Start mock pipeline
        run: echo "Starting mock pipeline..."

      - name: Wait 2 seconds
        run: sleep 2

      - name: Finish mock pipeline
        run: echo "Mock pipeline finished after 10 seconds."

